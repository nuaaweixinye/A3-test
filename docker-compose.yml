# 一键部署：docker compose up --build
# 仅前端容器（无数据库）。透传讯飞星火密钥等环境变量（可改用 .env 文件 + env_file）。
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: learning-multiagent
    ports:
      - "3000:3000"
    environment:
      # 讯飞星火（OpenAI 兼容）；未设置则进入 mock 模式，仍可运行
      SPARK_API_KEY: ${SPARK_API_KEY:-}
      SPARK_BASE_URL: ${SPARK_BASE_URL:-https://spark-api-open.xf-yun.com/v1}
      SPARK_DEFAULT_MODEL: ${SPARK_DEFAULT_MODEL:-4.0Ultra}
      MODEL_ROUTES: ${MODEL_ROUTES:-}
    restart: unless-stopped
